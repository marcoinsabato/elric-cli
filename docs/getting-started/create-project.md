# Create Your First Project

Create a project:

```bash
elric new my_app
cd my_app
```

This command uses the official template repository and creates a complete backend project scaffold.

## What you get

- `app/` for routes, middleware, controllers, AI modules
- `database/` for models and migrations
- `config/` for settings and logging
- `.env.example` for local configuration

